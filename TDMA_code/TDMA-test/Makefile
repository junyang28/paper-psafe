COMPONENT=TdmaAppC

CFLAGS += $(INCLUDES)

PFLAGS += -DTIMESYNC_RATE=1000
PFLAGS += -I$(TINYOS_OS_DIR)/lib/ftsp
CFLAGS += -DCC2420X_DEF_RFPOWER=31

TINYOS_ROOT_DIR?=../..
include $(TINYOS_ROOT_DIR)/Makefile.include
